# Tenji — Photo Exhibition Planner

A simple, pure-frontend webapp to plan a photo exhibition: define walls (in cm),
place photos at real-world print sizes, arrange them with alignment guides, and
export a layout image plus a print-shop size table.

Personal prototype. Vite + React + TypeScript, data stored locally in the browser (IndexedDB).
