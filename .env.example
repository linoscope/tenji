# Supabase config for share-by-link (publishable key is browser-safe; protected by RLS).
# Copy to .env and fill in. The Share UI stays hidden when these are unset.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
